#include <bits/stdc++.h>
#define ll long long
#define sws cin.tie(0) -> sync_with_stdio(false);

// problem < n²

class Solution 
{
public:
  pair<int, int> minimum_flights() {

    make_pair(0, 0);
  }
}

int32_t main() {
  int n, m; std::cin >> n >> m;
  // n - number of countries
  // m - number of flights

  int a, b, k; std::cin >> a >> b >> k;
  // k - k-tickets available
  // a -> country where bob lives
  // b -> country where alice lives

  while(m--) {
    int u, v, w; std::cin >> u >> v >> w;
    // coming from u
    // going to v
    // w is the cost
    int distance = 


  }
  


  return 0;
}